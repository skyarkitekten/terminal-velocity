[project]
name = "terminal-velocity"
version = "0.1.0"
description = "Terminal Velocity is a reference implementation that applies GitOps principles to building and deploying agents to Microsoft Foundry."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
